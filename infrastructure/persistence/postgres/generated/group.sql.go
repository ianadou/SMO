// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: group.sql

package generated

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createGroup = `-- name: CreateGroup :one

INSERT INTO groups (id, organizer_id, name, created_at)
VALUES ($1, $2, $3, $4)
RETURNING id, organizer_id, name, created_at
`

type CreateGroupParams struct {
	ID          string
	OrganizerID string
	Name        string
	CreatedAt   pgtype.Timestamptz
}

// ============================================================================
// group.sql — Queries for the groups table.
//
// Each query is annotated with `-- name: FunctionName :returnType` so sqlc
// generates the corresponding Go function in
// infrastructure/persistence/postgres/generated/group.sql.go
//
// Return type annotations:
//
//	:one      → returns exactly one row (error if zero or more than one)
//	:many     → returns zero or more rows as a slice
//	:exec     → executes the query and returns only an error
//	:execrows → executes and returns the number of affected rows
//
// ============================================================================
func (q *Queries) CreateGroup(ctx context.Context, arg CreateGroupParams) (Groups, error) {
	row := q.db.QueryRow(ctx, createGroup,
		arg.ID,
		arg.OrganizerID,
		arg.Name,
		arg.CreatedAt,
	)
	var i Groups
	err := row.Scan(
		&i.ID,
		&i.OrganizerID,
		&i.Name,
		&i.CreatedAt,
	)
	return i, err
}

const deleteGroup = `-- name: DeleteGroup :exec
DELETE FROM groups
WHERE id = $1
`

func (q *Queries) DeleteGroup(ctx context.Context, id string) error {
	_, err := q.db.Exec(ctx, deleteGroup, id)
	return err
}

const getGroupByID = `-- name: GetGroupByID :one
SELECT id, organizer_id, name, created_at
FROM groups
WHERE id = $1
`

func (q *Queries) GetGroupByID(ctx context.Context, id string) (Groups, error) {
	row := q.db.QueryRow(ctx, getGroupByID, id)
	var i Groups
	err := row.Scan(
		&i.ID,
		&i.OrganizerID,
		&i.Name,
		&i.CreatedAt,
	)
	return i, err
}

const listGroupsByOrganizerID = `-- name: ListGroupsByOrganizerID :many
SELECT id, organizer_id, name, created_at
FROM groups
WHERE organizer_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListGroupsByOrganizerID(ctx context.Context, organizerID string) ([]Groups, error) {
	rows, err := q.db.Query(ctx, listGroupsByOrganizerID, organizerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Groups
	for rows.Next() {
		var i Groups
		if err := rows.Scan(
			&i.ID,
			&i.OrganizerID,
			&i.Name,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateGroup = `-- name: UpdateGroup :one
UPDATE groups
SET name = $2
WHERE id = $1
RETURNING id, organizer_id, name, created_at
`

type UpdateGroupParams struct {
	ID   string
	Name string
}

func (q *Queries) UpdateGroup(ctx context.Context, arg UpdateGroupParams) (Groups, error) {
	row := q.db.QueryRow(ctx, updateGroup, arg.ID, arg.Name)
	var i Groups
	err := row.Scan(
		&i.ID,
		&i.OrganizerID,
		&i.Name,
		&i.CreatedAt,
	)
	return i, err
}
