// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: organizer.sql

package generated

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createOrganizer = `-- name: CreateOrganizer :one
INSERT INTO organizers (
    id, email, password_hash, display_name, created_at
) VALUES (
    $1, $2, $3, $4, $5
) RETURNING id, email, password_hash, display_name, created_at
`

type CreateOrganizerParams struct {
	ID           string
	Email        string
	PasswordHash string
	DisplayName  string
	CreatedAt    pgtype.Timestamptz
}

// Inserts a new organizer. The UNIQUE constraint on email rejects
// duplicates; the repository translates that violation into
// ErrEmailAlreadyExists.
func (q *Queries) CreateOrganizer(ctx context.Context, arg CreateOrganizerParams) (Organizers, error) {
	row := q.db.QueryRow(ctx, createOrganizer,
		arg.ID,
		arg.Email,
		arg.PasswordHash,
		arg.DisplayName,
		arg.CreatedAt,
	)
	var i Organizers
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.CreatedAt,
	)
	return i, err
}

const getOrganizerByEmail = `-- name: GetOrganizerByEmail :one
SELECT id, email, password_hash, display_name, created_at FROM organizers WHERE email = $1
`

// Email lookups go through this query exclusively. Emails are stored
// lower-cased by the entity, so the WHERE clause does not need
// LOWER() — but we still pass the lowered value at the application
// layer for safety.
func (q *Queries) GetOrganizerByEmail(ctx context.Context, email string) (Organizers, error) {
	row := q.db.QueryRow(ctx, getOrganizerByEmail, email)
	var i Organizers
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.CreatedAt,
	)
	return i, err
}

const getOrganizerByID = `-- name: GetOrganizerByID :one
SELECT id, email, password_hash, display_name, created_at FROM organizers WHERE id = $1
`

func (q *Queries) GetOrganizerByID(ctx context.Context, id string) (Organizers, error) {
	row := q.db.QueryRow(ctx, getOrganizerByID, id)
	var i Organizers
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.CreatedAt,
	)
	return i, err
}
